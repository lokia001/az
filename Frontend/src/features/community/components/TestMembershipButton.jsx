// Test component để kiểm tra import
import React from 'react';
import Button from 'react-bootstrap/Button';

const TestMembershipButton = ({ communityId, communityDetail }) => {
    return (
        <Button variant="primary">
            Test Button
        </Button>
    );
};

export default TestMembershipButton;
